### init repository (from git)

$ git clone git://github.com/gorthauer/QtDockTile.git 
$ cd QtDockTile
$ git submodule update --init

### build

$ mkdir build
$ cmake ..
$ make 

### install

$ sudo make install

### uninstall

$ sudo make uninstall